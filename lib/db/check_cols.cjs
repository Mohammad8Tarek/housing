const { Pool } = require('pg'); const pool = new Pool({ connectionString: 'postgres://postgres:postgres@localhost:5432/housing_db' }); pool.query('SELECT column_name FROM information_schema.columns WHERE table_schema = \'prop_1\' AND table_name = \'employees\'').then(r => { console.log(r.rows.map(row => row.column_name)); process.exit(0); });